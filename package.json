{
  "name": "dsh-client",
  "version": "1.0.0",
  "private": true,
  "description": "Local desktop client that launches and supervises the DeepSeek Harness (dsh) web service, replacing the manual `dsh web` + browser flow.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "debug": "electron . --enable-logging"
  },
  "devDependencies": {
    "electron": "^43.4.1"
  }
}
